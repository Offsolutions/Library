@model IEnumerable<LibraryManagement.Models.Batch>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create","",new{ @class="btn btn-space btn-primary"})
</p>
<table class="table table-striped table-hover table-fw-widget" id="table5">
 <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BatchNumber)
        </th>
        <th></th>
    </tr>
	 </thead>
	 <tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BatchNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Bid },new {@class= " badge badge-primary" }) |
            @Html.ActionLink("Details", "Details", new { id=item.Bid },new {@class= " badge badge-warning" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Bid },new {@class= " badge badge-danger" })
        </td>
    </tr>
}
</tbody>
</table>
